class Tool < Thor
  desc "A tool to reduce the point in kml file", "File path pass as a argument"
  def reduce(file)
    puts "Ready to process..."
    puts "Please give a file full path here"
  end
end